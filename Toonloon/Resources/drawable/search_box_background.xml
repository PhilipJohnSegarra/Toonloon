<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="#0B1237" />

    <!-- Border color and width -->
    <stroke
        android:width="1dp"
        android:color="@android:color/white" />

    <!-- Corner radius -->
    <corners android:radius="8dp" />
</shape>
